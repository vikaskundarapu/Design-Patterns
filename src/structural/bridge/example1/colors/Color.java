package structural.bridge.example1.colors;

/**
 * Design a color-oriented interface that is minimal, necessary, and sufficient.
 * Its goal is to decouple the abstraction from the color. Define a derived
 * class of that interface for each color.
 */
public interface Color {
	public void applyColor();
}
