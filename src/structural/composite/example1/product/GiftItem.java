package structural.composite.example1.product;

/**
 * Create a common interface which declares a method to print the gift item.
 */
public interface GiftItem {

	public void printPackagingLevel(int level);

}
